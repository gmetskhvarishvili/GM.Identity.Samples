using System.Resources;

// The embedded .resx resources in this project are authored in English, so declare it as the
// neutral (fallback) culture. This lets the ResourceManager skip the satellite-assembly probe
// for English callers and satisfies analyzer rule CA1824.
[assembly: NeutralResourcesLanguage("en")]
